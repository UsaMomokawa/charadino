{
  "tasks": {
    "dev": "deno run --no-check --allow-all --watch mod.ts",
    "start": "deno run --no-check --watch mod.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "test": "deno test"
  }
}
